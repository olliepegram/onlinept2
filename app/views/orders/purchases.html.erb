<div class="container">

<div class="center">
<h2>Your Purchase History</h2>
</div>

<table class="table table-striped table-bordered">
    <tr>
        <th class="center">Image</th>
        <th class="center">Item</th>
        <th class="center">Price</th>
        <th class="center">Seller</th>
        <th class="center">Dates Purchased</th>
        <td class="center">Download</th>
    </tr>

    <% @orders.each do |order| %>
    <tr>
        <td><%= image_tag order.listing.image.url(:thumb) %></td>
        <td><%= order.listing.name %></td>
        <td><%= number_to_currency(order.listing.price) %></td>
        <td><%= order.seller.first_name %></td>
        <td><%= order.created_at.strftime("%B %-d, %Y") %></td>
        <td><%= link_to "Download", order.listing.file.url %></td>
        <% end %>
    </table>
</div>
